
SpecStudio Issues 

On the project listing, the files should appear with the folder they are in.


When converting, the outputs should be in the corresponding folders.    The spectable file should also be copied to the output folder.  

On the attribute table, one more optional column - Multiples - If muliples, the attribute will be a List<DataType> or List<AttributeSet>. 
The corresponding Define can have either 
| Value1 | Value2 |     for a DataType 
or 
multiple lines for an AttributeSet 

Claude does not keep passwords except in the ini file 

The convertor should put the FromString in the Typed class.   That way the string class
is pure.    

Put all features, converted to spectable types, in a test project.    
Make up a production directory for each language and put the stuff in there.  Then try 
it all out. 

Read the documentation for GE and make sure that most everything is included in SpecTable.

Have a CompareOnly option on the table - so the equals (for string version) does not compare any fields that are not shown.  


If you have an existing entity class, you can make up a static Entity convert(entitytyped)
and vice versa for use in the test.   This should either construct or build (however entity is defined) an Entity object.    And there EntityTyped convert(Entity).   A skeleton for
these is in the data directory or data file. 

Note data types should have either a constructor that takes a string or a method that takes a string and returns an object.   
**** May need a directive about this - additional includes, etc.      
	Actually may not need this - let the developer turn Typed into the Object.  

Convert inserted .csv file into a table.   Check that the fields match the attributeset 


Should there be a ListOfString for the multi-line string?

Add to/from JSON and table to/from JSON to the string class.

Maybe add the table Define  instead of having lots of Define  -  meaning is the same.   
Define 
| Name       | Value | Notes                 |
| HIGH_VALUE | 100   | Highest allowed input |
| LOW_VALUE  | 1     | Lowest allowed input  |

Should there be calculated values?    (=X+=Y)  as the value?   Think about this 

=HIGH_VALUE 
      


In documentation, show the two alternative ways a single value can be input 







