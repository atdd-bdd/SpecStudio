Remaining Work — SpecStudio / SpecTable
========================================

Altered so that it is separated into Issues and Future Features 


## Doing


### Make up a project with all the feature files from GherkinExecutor and try
  it out as a migration exercise.



## Holding:

### Maybe add // as a comment indicator.    

### Bowling project: bowling.spectable exists at the repo root with rich
  scenarios (roll validation, scoring, frame display, game-complete checks),
  but there's no generated code/glue anywhere yet and it isn't wired to run
  from the IDE. Still need: generate, wire up glue, run for a passing and a
  failing case, then change the spec and re-run.

### True Decimal type separation for C++/Go/JavaScript/Rust: all 4 now correctly
  recognize "Scientific" and "Decimal" as distinct keywords (previously
  Scientific silently produced invalid output — e.g. C++/JS would emit a
  reference to a nonexistent "Scientific" type), but their runtime
  representation for Decimal is still the same double/float64/number/f64
  used for Scientific, since none of these 4 have a zero-dependency native
  fixed-precision type. C# (native `decimal`) and Python (stdlib
  `decimal.Decimal`) got the real distinct type — see Done below. If true
  Decimal precision is wanted for the other 4, it needs a design decision
  first: Go/Rust could get a cheap newtype (`type Decimal float64` /
  `struct Decimal(f64)`), but C++/JavaScript would need a small wrapper
  class introduced somewhere shared, which doesn't exist yet for either.

  *** Will hold for now.   

## Future Enhancements: 

### Have a tab / untab (indent/outdent) command for the file; decide whether
  there should also be a whole-file reformat/indent-normalize command. Not
  implemented — LineNumberEdit::keyPressEvent (LineNumberEdit.cpp:525-546)
  only special-cases Tab/Backtab for the autocomplete popup.

### Copy named comments in the .spectable into comments in the generated code.

### Internal Markdown editor.

### Cleanup -  

  #### Analyze-implementation command: flag glue-code methods that are never
  referenced by any step. Not implemented.

  #### When a step name changes, rename/update the corresponding glue method name
  on the next build/analyze instead of leaving an orphaned method. Not
  implemented — AppController::onRenameStep (AppController.cpp:1386) exists
  but only does a find/replace across the .spectable source; it never
  touches already-generated glue method names.

  #### Remove common/ files that are no longer referenced -- DONE.
  Build > Project and Build > Solution now delete every generated
  *String/*Typed file from each output common/ folder before converting
  anything (AppController::clearGeneratedCommonClasses). A whole-project build
  converts every .spectable it owns, so all live classes are written again;
  whatever does not come back belonged to a renamed or deleted AttributeSet.
  Build > Current File deliberately does not do this -- one spec cannot tell
  which classes belong to the specs it was not given.

  The delete runs as a separate pass over all targets before the first
  converter starts. BuildController::run returns as soon as the process is
  spawned, so every conversion is in flight at once and deleting from inside
  that loop would throw away files a sibling had already written.

  Only *String/*Typed go. The JSON reader, the index/module file, YesNo,
  TableHelper and ProductionHelper stay -- they are not per-AttributeSet, and
  converting a single file still expects to find them. The merged index prunes
  itself (see SourceScan.h), so index and files stay in step.

  Two consequences worth knowing:
    - It only clears the output folder of the language whose .specconfig is
      active, so clearing a project takes one build per configuration. Both
      downstream trees have now had all nine run: SpecStudioExampleTests lost
      Pricing, Discounting and Shipping in every language (plus SomeTypes,
      LabelValue and EnumerationValues in Java, which was further out of date),
      and SpecStudioVariousTests turned out to have orphans only in Java.
    - A dead glue method becomes a compile error instead of staying invisible.
      First run deleted PricingString/PricingTyped -- Pricing is in no spec any
      more -- and Shopping_Cart_glue.java still had Then_result_is taking a
      List<PricingString>. That is the intended signal (it is the same dead
      glue the analyze-implementation item above is meant to flag), but it does
      mean the first build after this change can fail to compile until the
      matching glue method is removed.

  #### Production classes are never overwritten -- verified in all nine
  languages: hand-edit a production file, regenerate, the edit survives; and
  with the "create production classes" checkbox off, zero production files are
  written. Working as intended.

  The skip-if-present test no longer keys on the filename. It now looks for a
  *declaration of the type* anywhere under the production folder
  (sourcescan::ProductionScan), so grouping several classes into one file --
  idiomatic Go, normal Rust, mainstream Python -- no longer gets a duplicate
  template written beside the real implementation. When the type is found in a
  differently-named file the run says so:
    INFO:0:Production type 'ShoppingCart' is already implemented in <path>
        - no template written
  Verified per language by concatenating every generated production file into
  one Domain.<ext>, deleting the per-type files, and regenerating: nothing is
  recreated and one INFO line appears per type.

  Still open: renaming an Entity in the spec orphans the implementation.
  ShoppingCart -> Cart leaves the developer's ShoppingCart.cs on disk and writes
  a fresh Cart.cs template; the real implementation is now in the file the tests
  no longer reference, with no warning. Detecting that needs a record of what was
  emitted under which name, so it belongs with the rename-aware cleanup above.

  Also cosmetic: the Python generator writes production/Address.py, where PEP 8
  wants a lowercase module name (address.py). Renaming it would break every
  existing import in both downstream repos, so it needs doing as its own change.

### Diff viewer: OutputPanel already has a real "Diff" tab (OutputPanel.cpp:232,
  AppController.cpp:500) but it's a generic git working-tree diff, not a
  comparison against "the .spectable version that generated the current
  tests" — that provenance tracking doesn't exist yet.

### Share Changes dialog is a real implementation now (see Done below) but is
  simpler than the full appendix mockup: no inline per-file diff preview in
  the dialog (use the existing Diff tab for that), no "Preview Details"
  button (description is optional, falls back to "Update" as the commit
  message if left blank — git requires a non-empty message). The full
  friendly error-recovery dialog (Retry Push/Fetch buttons, Open Merge Tool,
  upstream status, etc.) also isn't built — push/fetch failures currently
  show a plain QMessageBox with detailed text rather than the richer mockup
  in the appendix.

### Define with Table 
Define 
| Name  | Value  | Notes                     |
| TBR   | -1     | Roll has not occurred     |
| TBS   | -1     | Score not yet computable  |


## Doing Later 



## Bigger Language Feature Ideas (not scoped/designed yet) ---

### Sequence keyword: shows a sequence of operations where each step's Given is
  the previous step's output.

### State table: State/Event grid (states down the side, events across the
  top) filled in with transitions; generates glue code for every
  transition. Shorthand: "Given this state / When this event / Then this
  state." A state's target could reference a Define containing an if-clause
  for more complex transition logic.

### Karnaugh map style DataType/table with don't-cares ("DNC"), checked for
  completeness.

### Business Rule table with complex per-field conditions (e.g. ">10, >-10,
  >10 & <12, =10 | =12" — and/or/not plus comparison operators) instead of
  plain examples; if the rule fields are typed, auto-generate the Examples
  rows from the condition table.

## Ideas / Open Questions ---





## --- Done ---

[x] "Uses" named comment (SpecTable v3.3.a): recognized as an optional, non-executable
    documentation line — `Uses <Comment>` — anywhere Description/Details/Constraint/Notes are
    already accepted (Entity, Collection, Define, Attributes, Given/When/Then/Vertical, etc).
    Parsed and silently skipped like its siblings — not stored/propagated anywhere yet, so it
    doesn't feed the separate "Copy named comments into generated code" item above. Added to:
    converter/SpectableParser.cpp (isSkipKeyword), SpecTableAnalyzer.cpp and SpecTableIndex.cpp
    (skip-line regexes so hover/diagnostics still find the table after a Uses line),
    SpecTableEditor.cpp (autocomplete), SpecTableHighlighter.cpp (named-comment coloring),
    BackgroundCleanupDialog.cpp and ScenarioSimulatorDialog.cpp (their own skipWords lists).
[x] Swift generator: new SwiftGenerator.h/.cpp (converter/), wired into main.cpp (-l Swift) and
    CMakeLists.txt. Modeled on RustGenerator's structure but using Swift idioms — camelCase
    methods/properties, XCTest test methods, no artificial module-qualification needed since
    Swift files in one target see each other automatically. Found and fixed a bug while
    verifying: the isValid property on generated ValidValues production classes listed ALL
    example values regardless of their IsValid column, so explicitly-invalid examples
    incorrectly reported as valid — fixed in both the new Swift generator and the RustGenerator
    it was copied from. Verified structurally (no Swift toolchain available on this Windows
    machine to compile) across numeric types, enums, ValidValues, and Collection production
    classes — output reviewed line by line for correctness.
[x] Folder mirroring extended to all remaining generators: previously only CSharpGenerator.cpp
    and JavaGenerator.cpp mirrored the .spectable's source subfolder into the output directory
    (via --source-root). Now Rust, Python, C++, JavaScript, Go, and Swift do too — the IDE
    already sent --source-root unconditionally for every language, so no IDE-side changes were
    needed. Found and fixed two more bugs while verifying by actually compiling/running the
    output: C++ and JavaScript generate file-relative #include/import paths that would silently
    break once a spec's output moved into a subfolder (now computes the correct ../ depth per
    nesting level), and a separate pre-existing bug in JavaScript's test file — it imported the
    wrong glue filename entirely, unrelated to mirroring, breaking every JS generation whether
    mirrored or not. Verified: all 7 generators produce clean mirrored output with a 2-level-deep
    test spec; compiled the mirrored C++ output with g++; resolved the mirrored JavaScript import
    chain with Node; ran the mirrored Python test end-to-end with real pytest (correctly
    discovers/runs from the nested folder, fails with the expected NotImplementedError for the
    unimplemented stub, not an import error).
[x] Production output directory split: clarified as intentional, not a gap — ProductionHelper.java
    contains test-support conversion helpers (TypedToProduction/ProductionToTyped), not production
    code, so it correctly belongs in common/ alongside the other generated test-support classes
    rather than in --prod-dir. No change needed.
[x] Git UX redesign per the appendix (pragmatic scope, see note above for what's simplified):
    new ShareChangesDialog (replaces the bare GitPushDialog, now deleted) shows a friendly file
    list grouped by project (each project has its own git repo — confirmed this is already the
    architecture, not changed), an optional description field (defaults to "Update" as the commit
    message if left blank), and a collapsed Advanced section
    (per-project branch name, "push immediately after sharing" checkbox default on). On push
    failure, shows a friendly QMessageBox with detailed git output rather than a raw error. New
    prompts: opening a solution now asks "Get Others' Changes?" (Yes/No, default Yes per your
    spec) and, if yes, runs the existing pull+conflict-resolution flow (AppController::onPull,
    which already had ConflictResolutionDialog wired up for merge conflicts). Closing the app now
    asks "Share Changes?" after the Save All prompt (Yes/No, default No — deliberately the
    opposite default from the open-time prompt, since pushing to a shared remote is a more
    consequential accidental-Enter than a local fetch; flag if you'd rather default Yes here too).
    Confirmed already true and unchanged: one git repo per project (Project owns its own
    GitClient, git init at project creation), and git username/password are stored only in local
    per-project AppSettings (QSettings), never elsewhere. Verified: full solution build succeeds;
    launched the app to confirm no startup crash; validated the git-status-porcelain parsing
    logic (Modified/Added/Deleted) against a real throwaway git repo.
[x] Table column check, both directions, with precise error/warning severity: missing column
    with NO default value on the field -> ERROR (halts compilation); missing column WITH a
    default value -> silent (default is used); extra/unmatched column not on the AttrSet ->
    WARNING but compilation continues. Implemented in resolveExamplesRows (also fixed a related
    bug: it never actually applied field defaults for missing columns, just left them empty) and
    in both header-based branches of resolveStepRows (Define-table and step-table), skipped when
    step.compareOnly is set since DNC already covers "don't care about this column". Verified all
    three cases: extra column warns and still compiles; missing-with-default compiles silently;
    missing-without-default halts with a clear ERROR message and correct line number.
[x] JUnit5 assertEquals argument-order bug: new buildAssertEquals() helper in JavaGenerator.cpp
    builds the framework-correct call — JUnit4's org.junit.Assert only has a message-FIRST 3-arg
    overload, while JUnit5's Assertions and TestNG's Assert both take the message LAST (TestNG
    additionally swaps to actual-first). genStubMethod/appendMissingStubs now take a framework
    parameter and thread it through. While verifying, also found and fixed a related bug in the
    same area: YesNo fields were never handled in toJSON()/fromJSON() (fell through to raw
    getString(), producing a JSON->Object type mismatch) — added explicit "yesno" branches
    alongside the existing enum/DataType dispatch. Verified by regenerating and compiling
    TestProject/Types.spectable (exercises ValidValues, Enumeration, and YesNo together) for
    JUnit5 (default) and checking the correct argument order for TestNG.
[x] Multiples column fully removed (Collection replaces it): Field::multiples deleted from
    SpectableModel.h; SpectableParser.cpp no longer sets it and instead emits a WARNING pointing
    users at Collection when a "Multiples" column header is present; all 7 generators
    (JavaGenerator, CSharpGenerator, CppGenerator, GoGenerator, JavaScriptGenerator,
    PythonGenerator, RustGenerator) had their List<Type>/Vec<Type>/etc. multiples-branch codegen
    removed, collapsing to the plain scalar path. Verified across all 7: regenerated a numeric
    test spec in every language with no warnings; compiled/ran the Java, C#, Python, and C++
    output directly (javac, dotnet run, python -c, g++) and ran the JavaScript output with node;
    visually confirmed clean Go and Rust output. Also confirmed the deprecation warning fires
    correctly and the previously Multiples=Y field now generates as a correct plain scalar.
[x] DocString indentation: Step/Define now record docStringIndent (the opening """'s column,
    SpectableModel.h) at parse time; SpectableParser.cpp's appendDocLine strips up to that many
    leading whitespace characters from each content line (leadingWsCount helper), so structural
    file-nesting indentation is removed while any indentation beyond the opener's column is kept
    as intentional relative formatting. Verified by generating a docstring with a deliberately
    deeper-indented inner line and confirming the Java text block preserves exactly that relative
    indent (JavaGenerator's emitTextBlock, which already dedented per-block, now works on already
    correctly-anchored content).
[x] Attributes/Entities must declare a known type: new checkAttributeFieldTypes() in
    SpecTableAnalyzer.cpp scans every Attributes/Entity table's Type/DataType column and warns if
    a value isn't a built-in, user DataType, Entity/Attributes/Collection name, or DomainTerm.
    Verified with a standalone test harness built directly against SpecTableAnalyzer/
    SpecTableIndex (both QtCore-only) — correctly flagged a typo'd type name and left valid
    built-in/DataType references alone.
[x] Glue method parameter-type conflict detection: collectGlueSigs() now takes an optional
    QStringList* conflicts and warns when two steps map to the same glue method name but expect
    different parameter types (e.g. two scenarios both saying "Given values are : X" where X is a
    different AttrSet each time) — previously the second occurrence was silently dropped, which
    generated a test file that failed to compile (List<X> where the glue only accepted List<Y>).
    Verified by generating such a spec: the converter now emits a WARNING at the colliding line,
    and compiling the previously-silent case confirms the real compile error it would have caused.
[x] Proactive prompt for ad-hoc tables: SpecTableEditor::checkAdHocTableAttributeSet()
    (SpecTableEditor.cpp) fires via the same Enter-key hook as autoInsertTableHeader() — when
    the user finishes typing a table under a bare Given/When/Then/And/WhenThen line with no
    ": AttrSetName", it prompts "Create AttributeSet..." (inserts the declaration using the
    table's own headers and links the step line via " : Name") or "Use Existing..." (picks
    from known Attributes/Entities and links the step line), or "Not Now". Complements the
    existing passive Analyze-time warning (checkStepsWithTableButNoAttrSet,
    SpecTableAnalyzer.cpp:638) which already flagged this case but couldn't fix it inline.
[x] Hide non-spec files in Solution Explorer: new AppSettings::showAllFiles()/setShowAllFiles()
    (default false); SolutionTreeModel now skips FileType::Other files unless the setting is on
    (SolutionTreeModel.cpp buildNodes()); View menu has a checkable "Show All Files" action
    (MainWindow.cpp) wired through AppController::onToggleShowAllFiles().
[x] DomainTerm name vs. DataType name: new checkDomainTermVsDataTypeNames() in
    SpecTableAnalyzer.cpp warns when a DomainTerm's name collides (case-insensitively) with
    either a user-declared DataType or a built-in DataType name (Character/String/Integer/etc.).
[x] Scientific/Decimal parity across all 7 generators: CSharpGenerator.cpp (Decimal -> native
    `decimal`, Scientific -> double), PythonGenerator.cpp (Decimal -> stdlib decimal.Decimal with
    conditional `import decimal`, Scientific -> float), CppGenerator.cpp/GoGenerator.cpp/
    JavaScriptGenerator.cpp/RustGenerator.cpp (both now explicitly recognized, previously
    Scientific was silently unrecognized in all 4 — e.g. C++/JS would reference a nonexistent
    "Scientific" type/class). Verified by generating Integer/Scientific/Decimal fields in all 7
    languages and actually compiling+running the C# and Python output (dotnet run / python -c)
    confirming three genuinely distinct runtime types (int/double/decimal in C#;
    int/float/decimal.Decimal in Python).
[x] Bare DataType production classes: JavaGenerator.cpp's production-class loop previously only
    generated classes for DataTypes with ValidValues/EnumerationValues Examples tables — a plain
    DataType with no Examples table (e.g. "DataType AccountRef" with just a Description/
    Constraint) got NO class generated at all, even though Typed-class codegen assumes one exists
    (parseExpr's "new TypeName(ref)"). Now generates the same String-constructor wrapper class
    for these too. Also fixed while verifying: toJSON()/fromJSON() didn't handle custom
    DataType/enum fields at all (fell through to raw obj.getString(), producing broken round-trips
    or, for enum fields, code that wouldn't compile) — added proper dispatch (new isUserDataType()
    helper distinguishes user DataTypes from built-ins) so custom DataType fields serialize via
    their .value and enum fields via .name()/.valueOf(). Verified by generating and compiling a
    bare-DataType test spec end-to-end.
[x] "Fail every test" config toggle (Java only, matching existing production-class precedent):
    new SpecConfig::failEveryTest, JavaGenerator::Options::failEveryTest, --fail-every-test CLI
    flag, and a "Test Scaffolding" checkbox in SpecConfigEditor. When enabled, every generated
    @Test method (Scenario and BusinessRule/Calculation/DataType) starts with a fail() call, so a
    fresh scaffold is all-red until each test is implemented. Verified by generating with and
    without the flag and compiling both outputs.
[x] WhenThen clause: intended semantics clarified — not special "chaining" logic, just a way to
    specify a table with multiple When/Then value pairs that all share the same Given as context
    (analogous to a Business Rule Examples table). WhenThen is already a fully recognized step
    keyword with zero special-casing anywhere (SpectableParser.cpp:32,58; not referenced at all
    in JavaGenerator.cpp) — it flows through the exact same generic step/table/glue machinery as
    Given/When/Then, which already delivers this. "But" is already gone from the step grammar too.
[x] Analyze auto-run on solution load AND after Save All: AppController::setSolution()
    (AppController.cpp:120-124) already auto-analyzes all projects on load via
    QTimer::singleShot, and loadSolution() reaches this through setSolution(); onSaveAll()
    (AppController.cpp:374-390) does the same after saving. Both predate this session
    (commit 7925072) — earlier audit passes missed it because they only checked
    loadSolution() directly, not the setSolution() call it makes.
[x] Java class naming: toClassName() (JavaGenerator.cpp:173-180) now produces true PascalCase
    (first letter of each word uppercased, words merged with no separator) instead of joining
    words with underscores — e.g. "Data Definition Error" -> DataDefinitionError_Test.java /
    DataDefinitionError_glue.java, and Scenario/Examples glue method names read as
    Scenario_SimpleTableWithIntBad instead of Scenario_Simple_Table_With_Int_Bad. Verified by
    regenerating TestProject/Data Definition Error.spectable and compiling the output.
[x] ":" and Type-column autocomplete: SpecTableEditor::refreshDynamicCompletions()
    (SpecTableEditor.cpp:1286-1323) feeds Entities+Attributes for the ":" dropdown and
    built-ins+DataTypes for the Type column; a real "Create a DataType block for it?" prompt
    exists and inserts a stub DataType block (SpecTableEditor.cpp:389-449). Minor gap:
    DomainTerms aren't included in the Type-column list, only built-ins+DataTypes.
[x] Given/When/Then steps without a table already generate a glue-code call — collectGlueSigs
    (JavaGenerator.cpp:1077-1078) explicitly handles the empty-AttrSet/no-table case with a
    void/no-parameter glue signature. Not broken as previously suspected.
[x] Attribute/Entity/DataType hover preview: SpecTableEditor::showHoverPreview uses QToolTip
    (SpecTableEditor.cpp:148-255) — a real, working hover implementation (hover was the design
    choice made, not a non-modal dialog).
[x] Spec name vs. file name mismatch warning: JavaGenerator.cpp:1826-1834 ("Item 11") already
    emits "INFO:0:Specification name 'X' does not match file name 'Y'" when they differ.
[x] File Open dialog for files outside the current project: AppController::onOpenFileDialog
    (AppController.cpp:1744-1753) uses an unrestricted QFileDialog::getOpenFileName wired to
    Ctrl+O; onOpenFile (AppController.cpp:1698) opens any absolute path, no project-membership
    check.
[x] Java: Scientific and Decimal are now distinct types (Decimal -> java.math.BigDecimal
    throughout javaType/javaBoxedType/isDataType/parseExpr/cellLiteral/cellConvertExpr/JSON
    read paths; Scientific/Float remain double). Verified by building SpecTableConverter,
    generating Java from a test .spectable with Integer/Scientific/Decimal fields, and
    compiling the output end-to-end (common/*String/*Typed, glue, and JUnit test file all
    compiled; javap confirmed distinct int/double/BigDecimal field types).
[x] Java: fixed invalid generated code when no namespace/package prefix is configured —
    genGlueFile/genTestFile previously emitted `package ;` and `import .ClassName_glue;`
    (invalid Java) in that case; now the package line and glue import are omitted and both
    glue + test land together in the default package. Found and fixed while verifying the
    Scientific/Decimal fix by actually compiling generated output.
[x] "=" Define-name dropdown: SpecTableEditor::refreshDynamicCompletions() (SpecTableEditor.cpp:1299)
    already adds "=" + defineName for every project Define; LineNumberEdit::updateCompleterWords()
    (LineNumberEdit.cpp:481-487) filters to just those entries in "=" context; wired to fire on
    file open/index rebuild (AppController.cpp:334,972,1573). Earlier audit pass had this wrong —
    it's already implemented, no work needed.
[x] --source-root: package and output subfolder derived from .spectable subfolder path
[x] domain package/folder renamed to common
[x] .spectable file copied to output folder alongside generated files
[x] Build log: converter errors now shown; output drained at process finish; build.log written
[x] .spectable first choice in New File menu
[x] Tags pass-through: @Tag / [TestCategory] emitted before each @Test / [TestMethod]
[x] BusinessRule, Calculation, DataType tests generated
[x] DataType step table bug: all rows treated as data (no false header skip)
[x] "I don't understand that word": unrecognized keyword warning in parser + analyzer
[x] Project context menu: Rename Project, Move Project
[x] Multiple .specconfig files per project with Build > Configuration submenu
[x] imports field in .specconfig (extra using/import statements) — editable, not read-only
[x] Active build config persisted per project in AppSettings
[x] C# folder mirroring: output mirrors source subfolder structure
[x] SpecTableConverter: --import repeatable option wired through to generators
[x] CSV Insert "file.csv" in step tables: reads CSV, validates headers against AttrSet, warns on mismatch
[x] Analyze scope: F7=Project, Shift+F7=Solution
[x] Build scope: F6=Current File, Shift+F6=Project, Ctrl+F6=Solution
[x] Solution Explorer: Copy / Paste / Rename / Move / Delete on files; Rename/Move on projects
[x] Save all unsaved files before Analyze and Build; save all on Exit
[x] Enum DataType handling: isEnumType() detects EnumerationValues Examples; TypeName.valueOf(ref) in parseExpr
[x] ValidValues DataType glue stub with try/catch NumberFormatException, assertEquals isValid vs !error
[x] Enumeration DataType glue stub with try/catch IllegalArgumentException, assertEquals count vs values().length
[x] Production classes: ValidValues -> Java class w/ String + optional numeric constructor; Enumeration -> Java enum;
    only written if file doesn't exist; auto-imported in Typed/glue; SpecConfigEditor UI + --prod-dir/--prod-package CLI
[x] *String pure data holder (no toTyped()); *Typed has TypedClass(StringClass s) conversion constructor
[x] Package name editable in configuration
[x] Generators for C#, C++, Go, Java, JavaScript, Python, Rust
[x] Collection keyword (v3.3): parser, model, index, analyzer, generators
[x] Shopping Cart example (basic entity-with-entity case; catalog + order items work; addresses/total still open, see above)
[x] JSON serialization: toJSON()/fromJSON() generated for entities, including nested AttrSet fields and multiples lists (JavaGenerator.cpp:600-664)
[x] Production classes for Entities: genProductionEntity generates constructor + inner Builder (JavaGenerator.cpp:1594,1622-1630)
[x] ProductionHelper class: <Entity>TypedToProduction/ProductionToTyped plus List<Typed><->List<Production> (JavaGenerator.cpp:1674-1761); only written if file doesn't exist (JavaGenerator.cpp:1983-2005)
[x] TableHelper class: genTableHelperClass writes TableHelper.java with toListListXxx static converters into common/ (JavaGenerator.cpp:1154,1173,1949-1954)
[x] Multiples column: f.multiples drives List<Type> codegen across Java, C#, C++, Go, Python, Rust, JS (e.g. JavaGenerator.cpp:486-658), including JSON list handling — SUPERSEDED: Collection keyword has replaced this feature; see Code Generation section above re: removing the old codegen path
[x] Character built-in datatype: javaType/javaBoxedType map character/char -> char/Character (JavaGenerator.cpp:64,80)
[x] "Not Written" dialog when destination is unreachable on build (AppController.cpp:724,793)
[x] Syntax highlighting uses Scientific, not Float/Double (SpecTableHighlighter.cpp:48) — generator still accepts float/decimal as synonyms, see Code Generation section above
[x] Hover preview for EnumerationValues/ValidValues attributes + auto-insert header row on "Examples: EnumerationValues/ValidValues" (SpecTableEditor.cpp:158-193, ~665)
[x] Refresh command on Solution Explorer / project window (MainWindow.cpp:150,236 File menu + F5; AppController::onRefreshSolution, AppController.cpp:1250)
[x] File menu: Save As (MainWindow.cpp:147; AppController::onSaveAs, AppController.cpp:349)
[x] Entity-containing-entity scenario proven out: TestProject/Entity with Entity.spectable — Entity Address nested in Entity TwoAddresses, two Defines, Then checks on nested fields, one level of nesting confirmed working

===============================================================================
Appendix: Share Changes / error-recovery dialog design
(core flow now implemented as ShareChangesDialog — see Done above for what's
simplified vs. this original mockup; kept here as the reference design)
===============================================================================

Share Changes (Commit + Push) / Get Others' Changes (Fetch)

Purpose: help the user review what changed, add a short description, confirm
sharing, and perform Commit + Push in one step — without exposing git jargon
to non-programmers, while still giving programmers what they expect.

Share Changes dialog layout:

1. Summary section (top)
   "You have changes ready to share. Review the updates below, then click
   Share." Shows count of changed items, optional "Last shared on..."
   timestamp, optional "up to date with others' changes" indicator.

2. Change list (center)
   Collapsible list of changed files (e.g. Customer.spec, Order.spec,
   Address.spec). Each expands to a friendly before/after diff view — no
   hunks, no patch syntax. Labels: Added (green), Removed (red), Updated
   (blue). Example:
     Customer.spec
       Name: Ken -> Kenneth
       Age: 42 -> 43

3. Description box (required)
   Label "Describe your changes" / subtitle "This helps others understand
   what you shared." This is the commit message, without calling it that.

4. Advanced options (collapsed, for programmers)
   Show actual git commit message; show commit hash preview; show branch
   name; checkbox "Push immediately after sharing" (checked by default);
   checkbox "Allow empty description".

5. Action buttons
   Primary: "Share Changes". Secondary: "Cancel", "Preview Details"
   (optional). Tooltip: "Saves your changes and makes them available to
   others. (Commit + Push)".

Behavior on Share Changes:
   Validate description -> Commit -> Push -> success toast "Your changes
   have been shared." If push fails: "Your changes were saved, but could
   not be shared. Try 'Get Others' Changes' first, then share again."
   (Programmers will recognize this as commit-succeeded/push-failed.)

Error Recovery dialog ("There Was a Problem Sharing Changes" / "...Getting
Others' Changes"):

1. Friendly summary: "Something went wrong while updating your project. You
   can try again, or ask a developer for help."

2. Simple explanation, one of:
   - Commit failed: "Your changes could not be saved safely."
   - Push failed: "Your changes were saved, but could not be shared with
     others."
   - Fetch failed: "The latest updates could not be downloaded."
   - Merge conflict: "Your changes conflict with updates from others."

3. "Details for Developers" (collapsed by default): real git error text
   (e.g. "Error: non-fast-forward update"), plain-language explanation,
   suggested fix (e.g. "Run: git fetch, then merge or rebase"), buttons
   View Diff / Open Merge Tool / Retry Push / Retry Fetch / Open Terminal,
   and metadata: current branch, remote branch, commit hash, upstream
   status, staged/unstaged changes, conflicting file list.

4. Conflict resolution section (if applicable): friendly message "Some of
   your changes conflict with updates from others. A developer can help
   resolve this." plus developer tools: Open Merge Tool, Show Conflicting
   Files, View Side-by-Side Diff, Abort Merge / Reset (developer only).

5. Action buttons: primary "Try Again", secondary "Cancel"; developer-only
   buttons Retry Commit / Retry Push / Retry Fetch / Open Merge Tool /
   Open Terminal / View Diff.

Related, not yet designed: Conflict resolution dialog, History/timeline
viewer, Branch creation dialog, Status panel, Git onboarding flow for
non-programmers.
